package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════════
// ═══  REDSHIFT — Reference UI palette (navy / violet / green)  ═══
// ═══════════════════════════════════════════════════════════

// ─── Backgrounds ───
val BackgroundNavy = Color(0xFF0A0E17)
val BackgroundNavyDeep = Color(0xFF060A12)
val BackgroundNavyMid = Color(0xFF111827)

// ─── Surfaces ───
val SurfaceGlass = Color(0x990A0E17)          // rgba(10,14,23,0.6)
val SurfaceGlassLight = Color(0xCC101725)
val SurfaceGlassBright = Color(0xCC1E2530)
val SurfaceCard = Color(0xFF1E2530)
val SurfaceCardBright = Color(0xFF243040)
val SurfaceInner = Color(0xFF171E29)
val SurfaceElevated = Color(0xFF232C39)

// ─── Borders ───
val BorderNavy = Color(0xFF323B47)
val BorderGlass = Color(0x33323B47)
val BorderSubtle = Color(0x22323B47)

// ─── Premium accents ───
// Kept on legacy AccentRed/AccentBlue aliases so existing UI code keeps working.
val AccentRed = Color(0xFF9D4EDD)             // premium violet
val AccentRedBright = Color(0xFFB46CFA)
val AccentRedSoft = Color(0x339D4EDD)
val AccentRedGlow = Color(0x449D4EDD)

val AccentBlue = Color(0xFF6A5AE0)            // premium indigo-blue
val AccentBlueBright = Color(0xFF8275FF)
val AccentBlueSoft = Color(0x336A5AE0)
val AccentBlueGlow = Color(0x446A5AE0)
val AccentCyan = Color(0xFF80E0FF)

// ─── Status ───
val StatusGreen = Color(0xFF00FF7F)
val StatusAmber = Color(0xFFF2B84B)
val StatusRed = Color(0xFFFF657D)

// ─── Text ───
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFF888E99)
val TextMuted = Color(0xFF697384)
val TextDim = Color(0xFF4E5765)

// ─── Legacy compat aliases ───
val RedPrimary = AccentRed
val RedGradientEnd = AccentRedBright
val PurpleSecondary = AccentBlue
val AmberWarning = StatusAmber
val WarningAmber = StatusAmber
val ErrorRed = StatusRed
val CyberBackground = BackgroundNavy
val CyberBackgroundDeep = BackgroundNavyDeep
val CyberCard = SurfaceCard
val CyberElevated = SurfaceElevated
val VpnSurfaceTertiary = SurfaceInner
val TextTertiary = TextMuted
val SuccessGreen = StatusGreen
val SuccessGreenSoft = Color(0x3300FF7F)
val VpnDivider = BorderNavy
val PremiumBlue = AccentBlue
val PremiumPurple = AccentRed
val PremiumViolet = AccentRedBright
val PowerButtonSurface = SurfaceCard
val GlowRed = AccentRedGlow
val GlowPurple = AccentBlueGlow
val GlowGreen = Color(0x3300FF7F)
val GlassCard = SurfaceGlass
val GlassBorder = BorderGlass
val BackgroundGraphite = BackgroundNavy
val BackgroundGraphiteMid = BackgroundNavyMid
val BackgroundGraphiteDeep = BackgroundNavyDeep
val SurfaceGlassLight0 = SurfaceGlassLight
val SurfaceCardSolid = SurfaceCard
val BorderGraphite = BorderNavy
val AccentNeonGreen = StatusGreen
val AccentGreenSoft = Color(0x3300FF7F)
val AccentGreenGlow = Color(0x4400FF7F)
val AccentGreenBright = StatusGreen
val AccentWarning = StatusAmber
val AccentError = StatusRed
val PremiumStart = AccentBlue
val PremiumEnd = AccentRed
val PremiumCyan = AccentCyan
val PlanetOcean = BackgroundNavy
val PlanetLand = SurfaceCard
val PlanetAtmosphere = AccentBlueGlow
val PlanetGlow = AccentBlueGlow
val SpaceDark = BackgroundNavyDeep
